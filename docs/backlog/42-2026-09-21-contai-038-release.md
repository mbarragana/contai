# CONTAI-038 — release liberado — 2026-09-21

Critério 18 do `CONTAI-038` (contagem de segurança antes do `db push`,
exigida porque a migration `0017` dropa `documento.retencao_11`) rodada no
banco remoto, com autorização do Mateus:

```
select count(*) from documento where tipo='nf_servico' and retencao_11 is not null
→ 1
```

Não bloqueia o release: a `0017` não faz backfill nenhum de
`retencao_na_nota` a partir do valor antigo — a coluna nasce `null` para todo
legado, e o app já trata `null` como "não foi perguntado" (pergunta pendente
na tela de detalhe), nunca como "nenhuma retenção". A única nota afetada
passa a exigir conferência manual do Mateus na tela do documento, que é o
comportamento correto e seguro por desenho, confirmado antes do push.

`npx supabase db push` aplicado logo em seguida (ordem do `CLAUDE.md`:
migration antes de `git push`, sempre), liberando os commits represados de
`CONTAI-038`, `CONTAI-006`, `CONTAI-034` e `CONTAI-037` para push de código.
