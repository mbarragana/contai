## Gate 4 do CONTAI-036 — 2026-08-24 — a primeira saída anual do produto fecha

Origem: Gate 4 do `/develop` sobre o `CONTAI-036` (fatia 2 do `CONTAI-025`),
com o diff ainda **não commitado** (trabalho do `lead-engineer` na árvore de
trabalho). Este Gate 4 foi feito pelo `po`, sem rodar git.

### Os 16 critérios originais — todos PASS

Reconferidos um a um contra código e teste (não só contra o que o
`lead-engineer` relatou): 591/591 Vitest, 5/5 `e2e/discriminacao.spec.ts`,
5/5 `e2e/privilegios.spec.ts`, `typecheck` limpo — os três re-executados
neste Gate, de forma independente, não só lidos do relato.

Único ponto de nota, não de falha: **critério 13(d)** ("gerar Pagamentos
Efetuados com terreno pago-sem-comprovante → não veta") está provado em
Vitest (`terreno.test.ts`, *"crit. 13d"*), não em E2E — porque a rota de
Pagamentos Efetuados não existe (está fora de escopo deste ticket, por
desenho: só os portões nascem aqui). Documentado no cabeçalho de
`e2e/discriminacao.spec.ts`. PASS, com a ressalva registrada no critério.

### Dois critérios novos, retrofit pela régua do CONTAI-004 (hoje)

Mesmo dia em que a regra "todo substantivo concreto descoberto depois do
Gate 1 vira critério antes de fechar" foi instalada em
`.claude/commands/tickets-req.md` (Gate 4 do `CONTAI-004`), apliquei a mesma
régua aqui — dois achados substantivos que só viviam em comentário de
código e no diário:

- **Critério 15** — a cláusula de composição material×mão de obra por ano no
  Bloco A (achado fiscal **no meio do Gate 1**, virou o parecer
  `docs/pareceres/2026-08-24-composicao-material-mao-de-obra.md`,
  `composicaoDoAno` e a suspensão nomeada). Incluído: a correção de Gate 2
  que faz a contagem de notas sair do mesmo laço da composição (o defeito de
  duas divergências — "0 notas… compõem o total" e nota contada sem pôr
  centavo no ano).
- **Critério 16** — a marca da porta única não pode ser forjada com um `as`
  que compila. Achado do `cto-obra` no Gate 2 rodada 1, provado com exploit
  plantado pelo `lead-engineer` (mesmo protocolo do `CONTAI-004`: introduzir
  a regra proibida, ver vermelho, reverter).

Os dois já tinham teste nomeado e comentário extenso no código — o que
faltava era o número no ticket, que é a diferença entre "documentado" e
"o que o Gate 4 confere sistematicamente".

### As três perguntas do gate

1. **`quantasNotas` e a forja deveriam ter sido critério desde o início?**
   Sim, pela mesma régua — viraram os critérios 15 e 16, retroativamente.
2. **O ticket está entregue?** Sim. A meta 2 (relatórios anuais prontos)
   deixa de estar bloqueada pela guarda instalada no `CONTAI-025` — existe
   agora uma rota real (`/obras/[id]/discriminacao/[ano]`) que gera o Bloco A
   e o §4.5, e os portões de Pagamentos Efetuados e aferição INSS nascem
   destravados por tipo (critério 8), prontos para as telas que os
   consomem virem em tickets próprios.
3. **A dor original está resolvida?** Sim, e sem ficar decorativa: o
   critério 16 do `CONTAI-025` (nenhuma saída anual pago-sem-comprovante sem
   nomear o que ficou de fora) tem consumidor real agora — a linha do §4.5
   em `app/obras/[id]/discriminacao/[ano]/page.tsx`, provada em E2E (a) e
   (b) contra o Postgres local.

### O que não foi reaberto, de propósito

- **Critério 3** (razão do Bloco B) — já corrigido nesta sessão, antes deste
  Gate. Não mexido de novo.
- **D55** (`composicaoDaDiscriminacao` pondera por ordem de `id`) — decisão
  já registrada em
  `docs/backlog/27-2026-08-24-defeito-vivo-composicao-material-mao-de-obra.md`:
  fica como dívida P2, porque corrigi-la muda número de tela em produção
  (`CONTAI-021`) e é escopo do `po`, não deste ticket.

### Estado

`docs/tickets/CONTAI-036.md` atualizado com os 16 critérios marcados PASS,
mais os critérios 15 e 16 novos, mais um "Log de Gates" no corpo do ticket
(mesmo padrão do `CONTAI-004`/`CONTAI-021`). **Não commitado** — cabe ao
Mateus (ou ao próximo passo do `/develop`) decidir a mensagem e rodar o
commit; este Gate não rodou `git`.

**Pendência para quando isto for commitado**: a linha do `025` na tabela de
`docs/tickets/README.md` (linha ~156) cita o `036` como a fatia 2 "travada
por portão real" — precisa de uma linha própria para o `036` com o hash,
igual às outras entradas ✅ da tabela. Não fiz essa edição aqui porque não
existe hash ainda (diff não commitado).
